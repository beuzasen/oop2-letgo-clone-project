using System;
using System.Collections.Generic;
using System.Text;

namespace LetGoCloneStudentProject.ViewModels
{  //for creating the following properties:
   //Email, Password, and the command SubmitCommand() that
   //we will use in the Login command button on the Login page. 
    internal class LoginViewModel
    { 
    }
}
